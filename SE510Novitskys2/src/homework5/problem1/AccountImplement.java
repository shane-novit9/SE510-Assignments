package homework5.problem1;

import java.util.*;
public class AccountImplement implements AccountService{
	@Override 
	public int createUser(String first, String last, String addr) {
		// Create user with first and last names as well as address 
		return 0;
	}

	@Override
	public void deleteUser(int userId) {
		// Delete user at userId
	}

	@Override
	public void updateUser(int userId, User updatedUser) {
		// Assign user at userId updatedUser
	}
}
