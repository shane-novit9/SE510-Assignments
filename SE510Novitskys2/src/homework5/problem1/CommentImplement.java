package homework5.problem1;

public class CommentImplement implements CommentService{

	@Override
	public int createComment(int blogId, String comment) {
		// Create comment for blog at blogId.
		return 0;
	}

	@Override
	public void deleteComment(int blogId, int commentId) {
		// Delete comment at commentId in blog at blogId.
		
	}

	@Override
	public void updateComment(int blogId, int commentId, String newComment) {
		// Assign current comment at commentId in blog at blogId
		// newComment.
		
	}
	
}
